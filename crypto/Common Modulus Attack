from Crypto.Util.number import inverse, long_to_bytes

n = ...
e1 = ...
e2 = ...
c1 = ...
c2 = ...

def egcd(a, b):
    if b == 0:
        return (1, 0)
    x, y = egcd(b, a % b)
    return (y, x - (a // b) * y)

a, b = egcd(e1, e2)

m = (pow(c1, a, n) * pow(c2, b, n)) % n
print(long_to_bytes(m))
